# Never commit secrets
.adoption-sheets-sa.json
.sheet_config.json
*.json.local

# macOS cruft
.DS_Store

# Local launchd plist (no longer needed, can be deleted)
com.kitaboo.adoption-push.plist

# Logs — ignore the noisy .log files from the old nightly refresh script,
# but keep the adapter audit trail (logs/adapter_runs.jsonl and
# logs/changes/*.json) so pipeline history commits back to the repo.
logs/*.log
*.log

# Python
__pycache__/
*.pyc
.venv/

# Editor state
.vscode/
.idea/
